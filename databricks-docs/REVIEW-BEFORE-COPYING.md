# REVIEW BEFORE COPYING — databricks-docs (UPDATE-19)

Target: `~/.databricks/aitools/skills/databricks-docs/SKILL.md`
Version: 0.1.0 → **0.2.0**

## Changes applied
1. **'Delta Live Tables' → 'Lakeflow Spark Declarative Pipelines'** (example scenario + wording).
   The DLT parenthetical is preserved once in the new branding section for discoverability.
2. **`databricks-spark-declarative-pipelines` → `databricks-pipelines`** everywhere (the
   "Always prefer..." paragraph, the example scenario step, and Related Skills).
3. **Added Lakeflow product family branding section**: Lakeflow Spark Declarative Pipelines,
   Lakeflow Jobs, Lakeflow Connect, Lakeflow Designer.
4. **Version → 0.2.0**.

## Reviewer checks
- Only `SKILL.md` exists for this skill (no references dir); single-file update.
- Confirm the sibling skill slug is actually `databricks-pipelines` in your installed set
  (it is listed as an available skill).
